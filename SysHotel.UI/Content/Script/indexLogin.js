function loader() {
    let divContainerBlock = document.getElementById("container-block");
    let loader = document.createElement("div");
    loader.id = "loader";
    divContainerBlock.appendChild(loader);
}